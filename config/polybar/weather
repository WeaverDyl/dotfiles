#!/bin/bash

ping -q -c 1 8.8.8.8 > /dev/null 2>&1 || exit;

output=$(curl -s wttr.in/?format\="%C%20%t" | sed -e "s/\b\(.\)/\u\1/g")

if [[ ${output,,} =~ error|500|504|unknown ]];
then
	echo "Weather Error"
else
	echo $output
fi
